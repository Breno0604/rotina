# Freebuff — configuração do Netlify
# A SPA usa roteamento por hash (#/…), então não precisa de redirects.
[build]
  command = "npm run build"
  publish = "dist"

# URL do backend Convex em produção (pública — não é um segredo).
# Deployment: https://dashboard.convex.dev/t/nobrebeq-gmail-com/rotina
[build.environment]
  VITE_CONVEX_URL = "https://earnest-panther-236.convex.cloud"
